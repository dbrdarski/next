# Snapshot — 2026-08-07

> **Status: CURRENT (subordinate).** A session record, not an authority. Where this conflicts
> with `IMPLEMENTATION-STATUS.md`, that file wins. Full reasoning for every entry is in
> `DECISIONS.md` under 2026-08-07.

**Suites (2026-08-08 addendum, latest):** 492 lib / 262 conformance / 13 machinery — all
green, 1 lib + 3 conformance ignored; conformance green in default and serial order.
Clippy `-D warnings` clean, fmt clean, manifest 19/19 OK.

15 commits. `6f56fd0` … `685b7ae`.

---

## 2026-08-08 addendum — A6 laziness arc

Hull versus eager union distribution is now resolved by the held-image path, including the
correlation work that was still missing in this snapshot. The rulebook keeps the cheap hull;
only routing forces a finite image. The image is a local `BranchSet` whose cells retain nominal
source assignments, so shared derivations join diagonally while independent equal-domain
parameters still cross. Match outputs preserve arrivals, and routing a derived value narrows
the original sources and derived locals together (BR-09).

Branch metadata no longer lives in `AnalysisContract`: calls, returns, structures and
recursive/fact boundaries collapse to ordinary contracts, keeping source allocation out of
memo keys (BR-15). The unratified 256-combination cutoff is removed; finite cell routing has no
fuel threshold, pinned at 289 cells. The initial whole-judgment retry remains deleted. See
`DECISIONS.md` 2026-08-08 and the updated `DRAFT-demand-stopping-and-branch-routing-v0-1.md` §15.

## 2026-08-08 addendum — immutable analyzer memos

All persistent analyzer memo families that exist today now belong to the same `Interner` that
owns their identities: layer-2 group resolution, proven facts, RT-09 single/multi region rows,
and local group construction. Queries and answers are immutable; first publication wins and a
hit reuses the same answer allocation. Fact and RT-09 queries share interned annotated capture
tuples and named-contract environments. The only remaining thread-local analyzer state is the
active recursive-settlement stack/set, which is transient control state rather than knowledge.

The earlier parameter-name repair was incomplete. A stronger witness used two otherwise
equivalent recursive functions named `cd` and `loop`: either compiled cold, but the second could
receive the first function's source-spelled rows across separate identity owners. RT-09's query now
includes the source representative retained by its canonical callee while rows remain
source-spelled, and the memo belongs to that representative's identity owner. AI-04 pins both
orders under one deliberately shared `Interner`; a machinery
gate pins that completed memos do not return to thread-local tables. No project identity enters a
query. Runtime calls remain never memoized, and the absent template/evaluation/subcontract cache
families remain owed.

## 2026-08-08 addendum — GR-03B exact tuple chain

The written tuple `[3, 2]` now forms the finite selected dependency chain
`[3, 2] → [2] → []`; GR-03B is live and accepted. The analyzer does not recursively solve
termination or execute the calls. Exact Match guards select the required path at each represented
state, slice transfer's structural singleton is canonicalized to the same interned tuple value as
a written literal, and those exact states become ordinary domain-indexed nodes in the shared fact
graph. Safety, completion, and return settle there; grounding separately reads strict length
descent.

At its release this increment consumed only the acyclic ending: every selected recursive edge had
to strictly reduce one flat exact tuple in a direct-self Pure function. The next addendum extends
the same finite graph with GR-11's cyclic ending. Nested-state minting, Mutators, mutual exact
chains, body-constant insertion, and the wider exact-chain license remain outside the candidate.

## 2026-08-08 addendum — GR-11 exact closed orbit

The exact tuple graph now preserves ordered required dependencies and detects a reached cycle.
The cycle is a refutation only if every earlier dependency on its path has an acyclic exact
closure and independently proves completion; otherwise the candidate declines. This is the
GR-11 completing-prefix condition, and it keeps GR-29 from manufacturing a refutation through
the unproven `stall([7])` prefix.

The evidence is no longer numeric-shaped: `Refutation.witness` is the canonical interned root
`ValueRef`, with a separate drift-away or closed-orbit certificate. The latter records the cycle
entry, ordered path, and completing prefixes. GR-03A therefore reports the written root
`[3, 7, 2]`, not the later `[7, 2]` cycle state; GR-22B reports `[7]` and records `f([])` as the
completing dependency before `[7] → [7]`. GR-03A and GR-22B are live. The following GR-20
addendum releases the last ignored measured Phase-GR coverage target.

## 2026-08-08 addendum — GR-20 derived segment descent

Represented String/Tuple equality now gives the region table an exact singleton row. The ordered
remainder for `s == "" ? 0 : f(s[1...])` is consequently the ordinary non-empty String region.
GR-08 reads that region's length lower bound and the direct tail slice's one-cluster drop, then
proposes `Kind(String)` as the shared fact carrier. Open slices with integer bounds preserve their
proven String/Tuple kind, so safety, completion, and return settle over that carrier without
executing recursive calls.

Negative pins keep an unguarded clamped tail loop, a zero-width tail, mutation, uncertain arrival,
and unproved lower bounds outside the candidate. General nested-helper segment exposure remains
ordinary-analysis breadth. GR-20 is live, and every measured Phase-GR specimen row is active; the
three remaining conformance ignores are unrelated author-gated rows.

---

## 1. Resolved

### 1.1 Author rulings implemented

**`++` is concatenation** — split from numeric `+`, then extended to tuples
(`6f56fd0`, `aa0fab1`).

This closed a live unsoundness rather than adding a feature. With one overloaded `+`, μ §8's
commutative reordering could rewrite *string* concatenation, so two functions that spelled a
seam differently canonicalized to one shape and interned to one value — **defining one
function changed what another computed**:

```
g = (s) => "y" + s ; g("x")                        →  "yx"
f = (s) => s + "y" ; g = (s) => "y" + s ; g("x")   →  "xy"    ← f changed g
```

`(s) => s + s` also compared equal to `(s) => 2 * s`, though the second traps on a String.
The frozen rewrite list needed no amendment — the rewrites were never wrong about arithmetic,
they were wrong about what `+` was. Tuple support later unblocked GR-29.

**Principle 9 stamped** — unproven termination is an error. Already implemented before today;
found and verified, and the docs corrected: the termination spec still reads "warns and
compiles, leaning: error," and is manifest-protected, so the ruling supersedes it.

### 1.2 Soundness fixes — the compiler was *wrong*, not merely incomplete

| Defect | Symptom | Commit |
|---|---|---|
| Recursion discovery stopped at the immediate callee | `wrap = (k) => [spin(k)]` **compiled**; every act body too, since acts are blocks | `a5c4e95` |
| Local functions did not resolve | a function defined in a block was invisible to the **whole** analysis, termination included | `21573fa` |
| RT-09's memo query omitted source spelling retained by its answer | analyzing one program **changed the verdict of the next**; the first parameter-only repair missed renamed recursive siblings | initial repair `a30529f`; complete correction 2026-08-08 |
| Block groups rebuilt per analysis pass | four distinct values for one source function — an identity-law violation | `685b7ae` |

**On the first:** `ground_demand` opened with `if !is_recursive(callee) { return; }`, which asks
whether a callee is *itself* on a cycle. A seat that merely **reached** recursion was never checked.
Fixed by late resolution: walk the body under the arrived domain to discover the calls it actually
makes, carrying their arrived arguments. When a call closes the active call cycle, expansion stops
and grounding adjudicates that cycle from its edge transformations and drift. The walk discovers
recursion; it does not recursively solve termination. Carrying the arguments down is what keeps
`run = (n) => [countDown(n)]` at `run(5)` accepted; asking grounding over `Top` would have rejected
it.

**On the third:** `InstanceKey` was built from the **α-renamed** shape, so `(n) => … n …` and
`(k) => … k …` key identically — while a cached row's `result` is an expression in the
*original* spelling. The analyzer bound `k`, the rows referenced `n`, nothing resolved. An
earlier fix that cleared caches per compilation was **expiry, not memoization**, and was
reverted on the author's objection. Adding parameter names fixed this witness but not the whole
dependency: renamed recursive siblings in separate identity owners reproduced it on 2026-08-08.
The memo is now owner-local and the complete query carries the canonical callee's retained source
representative until the answer becomes a canonical row template.

### 1.3 Precision fixes

**A12** (`bb7a246`, `386c454`) — μ §8's arithmetic slice moved into the normalization phase, so
the oracle, the analyzer, and shape identity all read one rewriting. Anchoring (operands holding
their position) was then narrowed to act bodies only: an accepted program has no bottoms —
Principle 9 rejects the hang, safety rejects the trap — so in pure code nothing observes operand
order. Restored `f() + g() == g() + f()` and `g() + g() == 2 * g()`.

The property harness was itself unsound and was fixed: both runs shared one interner, so the
normalized module re-executed the raw closures and every body-level rewrite passed by
construction.

**Consequence suppression** (`10718b7`) — an operation whose operand already produced an Error
records nothing of its own. `d = (1 + "x") + (2 * "y")` went from three findings to one.
Siblings still all report; only an *Error* suppresses, so an Unproven operand still earns the
seat's rejecting Error.

### 1.4 Phase GR built (`0958053`)

26 specimen rows against the Grounding Specification's §15 table, where there were **zero**.
Three predicates encode §15's vocabulary; the "unproven" rows assert rejection **and** that no
refutation was minted — the property specimens 10, 17 and 29 exist to protect.

It found four gaps in **three different subsystems**, only one of them grounding. It also caught
a vacuous assertion in its own `gr_30` (`all()` over an empty vector passes).

---

## 2. Open

### 2.1 The gap map — 7 ignored rows

**Four Phase GR rows** (each a test asserting what the spec says, against an implementation
that does not do it):

| Row | State |
|---|---|
| `gr_03a` | **released 2026-08-08** through GR-11's closed-orbit route |
| `gr_03b` | exact-chain license does not fire for a tuple peeled by slice |
| `gr_20` | derived-segment read does not fire — false rejection |
| `gr_22b` | **released 2026-08-08** through GR-11's closed-orbit route |

**Three pre-existing**, all gated on author rulings: `a_acc_contract_claim_layer`,
`a_wrk_family_grids`, `snd3_world_decided_gray_runner`.

The ignored-row flaw was demonstrated and then corrected for GR-19 and GR-26: both improved
behind stale ignores, were rerun directly, and are now live. GR-30 is live too after grounding's
positive certificate view learned that a one-statement act block has the statement's completion
behaviour; the ordinary sequencing table and the refutation view remain unchanged. GR-08 is live
after the constant single-parameter mutual candidate gained edge-labelled simple-cycle composition
and member-specific safety orbits. The four remaining targets should likewise be remeasured
directly whenever adjacent machinery changes.

### 2.2 Multi-parameter body safety — carried and numeric-payload slices closed

The 2026-08-07 measurement below is superseded. These carried-coordinate examples now compile:

```
gcd = (a, b) => b == 0 ? a : gcd(b, a % b)      → compiles
f   = (a, b) => b == 0 ? a : f(a, b - 1)        → compiles
f   = (a, b) => a == 0 ? b : f(a - 1, b)        → compiles
f   = (a, b, c) => c == 0 ? a : f(a, b, c - 1)  → compiles
```

The local Fibonacci row also compiles and is live as GR-26. For a changing numeric payload,
GR-19 now proposes one fact domain only after the operation rulebook proves every written payload
edge safe and closed over Number:

```next
sumUntil = (n, acc) => n <= 0 ? acc : sumUntil(n - 1, acc + n)
```

`n` receives the existing descent envelope; `acc` is not a termination measure. The vector pass
still proves the body universally over the proposed domain, and a nonnumeric-edge twin proves the
proposal declines. General changed-domain/call-edge derivation remains owed; unsupported shapes
stay honestly Unproven.

Historical local-recursion probe:

```
cd = (k) => k == 0 ? 0 : cd(k - 1) ; r = cd(5)                → compiles
cd = (k) => …  ; run = (n) => { cd(n) } ; r = run(5)          → compiles
run = (n) => { cd = (k) => k == 0 ? 0 : cd(k - 1) ; cd(n) }   → compiles
```

The late local-call route now resolves this through the same canonical function identity and stops
at the reached back-edge. No `GroupTemplate`, source group identity, or cyclic symbolic closure
metadata is involved.

### 2.3 Decisions waiting on the author

- **The analysis/evaluation split** — normalize for identity only, and let the oracle evaluate
  the form as written. Would close the rejected-program trap-class residue and restore
  source-level diagnostic sites, at the cost of A12's premise.
- **Spans, or printing the normalized form.** A12 dissolves source-level sites: after
  normalization `(1 + "x") + (2 * "y") + (3 * "z")` has no `1 + "x"` node, so diagnostics lose
  it. The author's own answer — *show the user the normalized form* — needs a kernel-AST printer
  (none exists) and a field on `Finding` (which today carries no location at all).
- **Converting the 8 GR rows** to live measured-behaviour tests.
- **A1** (the sampler), and whether to stamp the finite-product exact-chain extension.

### 2.4 Normative text superseded by ruling

Recorded once, **not** tracked as open: the grammar and compendium still describe `+` as
overloaded and list `++` in the excluded-operator ledger; the termination spec still reads
"warns and compiles." All manifest-protected. The rulings supersede them.

---

## 3. Method notes

Three errors this session shared one root: **conclusions stated before measurement**, and each
error made the work sound more finished than it was.

- The trap-class residue was called "narrow, all `OperationSafety`" — measured, 44 of 84 chains
  changed class.
- The termination hole was reported twice at the wrong size: "the mutation world is uncovered,"
  then "act bodies aren't walked." It is neither — any diverging call inside a larger
  expression, `[spin(k)]` included.
- `Refutation.witness` being a `Rational` was named as the blocker for GR-03A/GR-22B. The real
  answer was that GR-11's refutation route had not been implemented; the field type was a
  consequence. The later GR-11 increment therefore changed it to canonical `ValueRef` while
  adding the route.

And one method failure the author caught directly: a fix was added, the **outcome** was checked
(did the verdict change?), the **mechanism** was not (did it do what it claimed?), and it was
reverted on that basis. Re-run with the mechanism instrumented, it worked exactly as claimed —
so the reversion had been reasoning from an unverified premise.

**The standing rule, now written into `DECISIONS.md`: verify the change did what it claims
before reading what it changed.**

**A caution on this session's numbers.** Several probes ran multiple programs in one process,
and before `a30529f` that could give a wrong reading — at least one case first reported as
rejected is accepted in isolation. Conformance rows, and anything re-measured in a fresh
process, stand. Loose probe output from earlier in the session should be re-run before it is
trusted.

---

## Post-snapshot continuation — 2026-08-08: recursive identity simplified

The author ruled that recursive groups are construction windows only. Canonical per-function code
applied to the immutable positional capture graph is the complete identity; recursive sibling/self
references are capture-graph edges. The imported `GroupTemplate`/member-slot/serialized-μ identity
path was deleted from both `oracle::mu` and analyzer cache keys. Facts and RT-09 rows now consume the
canonical function `ValueRef` directly. SCC discovery remains only to schedule joint knot tying.

Behavioral pins prove rename and member-order invariance, distinct-body separation, and collapse of
`a = () => b(); b = () => a()` to one pointer/fact identity. The machinery gate forbids restoring a
group serializer while requiring `GroupWindow` scheduling to remain. Full implementation details
and the manifest-protected normative discrepancy are recorded in the newest `DECISIONS.md` entry.

## Post-snapshot continuation — 2026-08-08: v0.6 and symbolic facts

The controlled normative revision is now published as
`docs/normative/next-mu-canonicalization-specification-v0-6.md`; the compendium, application
package, semantics companion, manifest, and agent guidance supersede the old template-group
mechanism explicitly. Symbolic analyzer instances now follow the same model as runtime values:
interned canonical per-function code applied to an interned positional capture-contract tuple.
Facts add the arrived domain and named contracts to that complete instance key; instance unions
are traversed member by member.

The initially recorded tail was corrected by the author on 2026-08-08: a locally recursive
function over an outer argument must not acquire cyclic symbolic closure metadata before the outer
call executes. The implemented route closure-converts direct local calls for analysis, carrying
the arrived outer contracts as ordinary leading arguments (`f(limit, n)`) and discovering
recursion only at the reached back-edge. Safety, return, completion, grounding, and enclosing-call
dependency discovery are wired through that closed analyzer identity; the conformance row is live.
