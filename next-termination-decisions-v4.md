# NEXT — Termination: Decisions & Specimens v4 (2026-07-29)

**Supersedes v3 (on disk as history). Hostile round 3 integrated — confirmatory on every round-2 item
(witness semantics, basin locality, the two-component certificate, witness-path forcing, the literal basin
grammar: all accepted as fixed), with one status blocker of its own: D-6's *ruling* and my *justification*
carried different statuses. Corrected here: the no-feature ruling stands [user]; the "tiers already
listen" equivalence is demoted to an owed read-path/coverage obligation; ranking-coordinate visibility is
distinguished from relational consistency. Rounds 1–2 findings: all confirmed and integrated ("verified"
is reserved for §16 discharge — vocabulary corrected per R3).** Structure: RULING / DERIVED SHAPE /
CANDIDATE / OPEN SPEC OBLIGATION. Provenance: **[user]** rulings · **[derived]** assistant work ·
**[R1]/[R2]/[R3]** round corrections · **[R4, 2026-07-29, in place]:** the world-decided **classifier**
restored as an owed Grounding-v1 procedure (the policy stands [user D-β]; "semantics fully derived /
blocker closed" was my overclaim — the third ruled-vs-procedure split this session); exact-chain
combinatorial bound theorem owed; witness wording clarified inside the ruling; status/terminology syncs ·
**[R5, 2026-07-29, in place — near-confirmatory]:** two proof-key precisions — the world-conditioned exit
is **reachable-and-jointly-realizable per domain-indexed SCC** (a function-global exit is not evidence:
the late-resolution law applied to the classifier itself); exact-chain finiteness covers the **complete
varying call state** (narrow v1 scope; control-location factor Q); terminology/footer/J3 syncs.
**[AUTHOR APPROVAL, 2026-07-29]: the author's review pass returned APPROVED — the [user]-tag inventory
(P-1; D-1…D-6; D-α/D-β; the three audit kills; the floor–stop catch) presented and confirmed in session;
this document is the ratified pre-spec source.** · **[FINAL CONFIRMATORY, 2026-07-29]: SUBSTANTIVELY ACCEPTED — three errata applied in place; no further
hostile termination-design round warranted [reviewer].** · **[R6, 2026-07-29, in place]:** two quantifier-completeness blockers — the WorldDecided admission needs
the **world-may-reach-completion closure to cover the full represented SCC domain** (one exitable
inhabitant admits nothing — round 2's wide-call lesson mirrored on the admission side); the exact-chain
state key gains **semantic-state stability** (pending Mutator writes made a terminating program falsely
orbit-refutable — the worst verdict class; v1 scope = pure/snapshot-stable recursion); two C§17
half-carries fixed, and a post-patch killed-term/stale-marker grep is now baked into the patch procedure.
**Filename identity:** this file is `next-termination-decisions-v4.md`; externally drifted copies
(v4-1, …) denote iterations of this one file — this header is the identity of record.

---

## 1. RULINGS [user] — all preserved; wording corrected per R1

**P-1 — Principle 9 status (compendium 1.0.9):** the warn-and-compile consequence for unproven grounding is
UNDER REVISION, unstamped, heavily leaning toward rejection (divergence is a *bug*). **Current law: unproven
grounding warns and compiles. Leaning: error.** Every consequence statement in this note reads "warning
today; error under the leaning" [R1 — the prior note's "blocking per P-1" contradicted P-1's own text].

**D-1 — all five automatic abilities in scope:** (a) arm-pruning by argument contract; (b) structural
descent; (c) the constant-drift core (direction, region/point bases, Mod grids, multi-base with a **fixed**
exit, mutual laps, backward traps); (d) forced exact fact-chains on literals under the **native exact-chain finiteness license** (§2.3); (e) lexicographic certificates — facts + kernel lemma + fixed enumeration, attached to
**grounding facts (C§10/13.2a/16), not C§9's ProgressStamp** [R1]. The stale "supplied measure" rescue
phrase is deleted [R1; D-6].

**D-2 — variable drifts admitted with a call-derived positive progress floor** (`sumUntil(1,0)`: step ≥ 1).
The licensing theory is §2.5's corrected Zeno clause — well-foundedness of the transition relation, not a
uniform runtime depth bound [R1].

**D-3 — DISSOLVED [user]:** no counting machinery. Take the recursive arm's input region, run **ordinary
non-recursive abstract contract analysis** [R1 wording — never "evaluation"] of the nested function's body
under it, read the derived segment-length facts (leading-A prefix grows ≥ 1, total length fixed), close by
D-2's clause. The compiler never invents quantities [user].

**D-4 — RE-RULED 2026-07-29 [user]: the native discipline confirmed; the derivation machinery deferred.**
Provenance corrected: the original "in" was ruled on assistant recommendation with the derivation
procedure's cost under-disclosed ("ingredients exist" — the one that did not was the derivation itself);
re-ruled by the author at full cost. **Confirmed (native, standing):** basin shapes are existing contracts
(`Geo(1,2)`, grids, `Repeat`/`Concat` forms — never a recursive-contract route [R1]); a certified basin
is checked at calls by ordinary membership/subcontract; **candidate-locality is law** — outside a basin,
that candidate contributes no conclusion [R2]; basin non-membership never manufactures a refutation or a
final unproven. **Deferred [user]: the automatic derivation** — the step-kind ↦ orbit-shape lookup table
(new rule inventory, each row owing its own soundness proof) is recorded as a **possible later
improvement** with its cost sheet, not v1 machinery. v1 consequence: collatz-class calls are honestly
unproven (no candidate source exists); diagnostics name the **blocking arm** (derivable from the failed
proof itself — error reporting, not machinery); the "narrow to X" remedy line returns if the improvement
ever lands. Deferral is verdict-neutral by candidate-locality: basins only ever add.

**D-5 — v1 refutation = drift-away + closed-orbit, both witnessed; the wide preserved-trigger presence
mechanism is STRUCK.** Corrected rationale [R1 high 9]: not "presence proofs are invalid" — exact evidence
proves presence routinely (`Equals([7])`, the orbit witness itself) — but: the algebra has **no admitted
existential contains-v family**; may-facts cannot establish universal persistence; payoff under P-1 is near
zero; witnesses and absence basins cover v1. Refutation forms carry explicit **must-conditions along the witness path** [R1 §12; R2]: a jointly
represented inhabited start whose actual transitions are forced and never reach base, justified by a
certified drift-away lasso or closed orbit — region-wide forcedness is required only for reusable *region*
facts, never for a call refutation.

**Witness semantics [R2 blocker — corrects the earlier "honest-unproven" verdict for wide calls]:** a
grounding obligation over a call contract is **universal over its represented inputs**, so it is **refuted
by one jointly realizable inhabitant carrying a valid nontermination witness** — the same shape as
operation safety over `Number | String` at a numeric seat. Mixed fates never decide a verdict: a mixed
region can never be *proven*, and is *refuted* exactly when such a witness is realizable and admitted by
the admitted witness set — **ruled [user, 2026-07-29]: no constructed witnesses; a witness is only
ever a call's own written argument** — clarified [R4, inside the ruling]: "written argument" means an
**exact value already represented by the argument expression's ordinary AnalysisContract** (literals;
exact aliases `x = [7]; f(x)`; exact correlated alternatives `f(flag ? [7] : [])`; ordinary-derived
exacts `[3] ++ [7] ⟶ Equals([3,7])`) — analysis-held facts, never synthesis; broad `Repeat(Number)` is
not represented-exact evidence (§2.3 walks those; the analyzer never assembles values it was not
handed — the candidate-input-generation import is killed); otherwise the verdict is **procedure-relative
unproven**, never "semantically forced unproven
because both fates exist." No existential `Contains(v)` family resurrects: `Equals([7]) ⊑ Repeat(Number)`
plus the §2.3 orbit certificate suffices, and the refutation diagnostic names the witness and the blocking
arm.

**D-6 — DISSOLVED [user]: no supplied-measure surface feature in v1.** Authors may expose a useful
quantity through ordinary program structure — an explicit argument, or an admitted compound expression
occurring in a base/guard [user direction: "if we specify, it should be solvable"]. **Grounding
Specification v1 must define the finite program-expression read path and its admitted coverage** (§4.7b);
unsupported quantities remain unproven; **incomplete coverage never implies an annotation feature** [R3
status split — the earlier "the tiers already listen" equivalence was assistant overclaim, demoted to the
owed obligation]. **Ranking coordinate ≠ relational invariant [R3]:** an explicit measure argument grounds
via ordinary argument-level drift when termination alone is the demand; any needed relation to the
original arguments (`m == 2a+b`) stays outside the [permanent] relational perimeter — the spec draws this
line explicitly, and nothing relational is smuggled in through the measure discussion.

**F-1 (finding, with D-6):** a supplied measure without landing analysis is unsound — the weighted-sum
specimen's `2a+b` drops by 1 every step and the function loops from (2,3) (crosses zero off the base point).
Point bases demand the grid discipline regardless of who names the measure — and the same (1,−2) state witnesses the **floor–stop coincidence obligation** (§4.7b): landing on the expression's floor is not stopping when the stop carries further conjuncts.

**Effect boundary, clauses 1–2 [user-derived, standing]:** the grounding obligation binds all recursion
uniformly — no act-kind exemption (the suppression hole closed by construction); world values participate
at their contracts (mixed loops prove via the internal certificate; pure-shaped forced loops inside Effects
refute). **Clause 3: policy RULED [user D-β]; the sound classifier OWED — see §3** [R4].

## 2. DERIVED SHAPES — direction settled, procedure/integration owed

**2.1 Lexicographic certificate.** Ackermann's joint-settlement theorem stands as proven at design level:
one well-founded induction on actual pairs carrying (terminates ∧ returns ⊑ R); descent hypothesis-free
(drift facts, first-component drop); a nested call's result gets membership from the IH's return half; the
compile-time joint hypotheses unroll into one non-circular semantic induction; `g(n)=>g(n+1)` is rejected
because descent-from-hypothesis violates (i). **Certificate attached to the grounding/return fact; the
lex-product lemma is a proof-kernel lemma (CRT precedent)** [R1 blocker 4]. Owed: the advance enumeration
inventory (argument subsets, permutations, orientations, max rank, domain-closure checks — deterministic finite,
not effort-dependent) [R1 §12]; §16 discharge of the theorem.

**2.2 Data basin — DEFERRED with D-4's derivation machinery [user, 2026-07-29].** The growth-derived
`Basin = Equals([]) ∪ (NotEquals(7) × Basin)` = `Repeat(NotEquals(7))` is the later-improvement's data
row; recorded, not v1. The **filtered case never needed it**: under the filter's output contract the merge
arm's guard is refuted per-region and the drain arm's length descent proves — existing machinery,
untouched. Queue item 1 (absence-form verification) remains load-bearing for the filtered case itself.

**2.3 Forced exact fact-chains — LICENSE, CONTENT DRAFTED (native form) [2026-07-29; rewritten after
the author's import audit — the prior "state carrier / node-count / cardinality N" framing was
explicit-state model-checking vocabulary, killed; remaining owed: placement in Grounding Spec v1 + round
scrutiny].** Mechanism [user-corrected lineage]: never evaluation — singleton domains force arm selection
through the region table; C§7 transfer on exact inputs yields the next singleton domain; recursive
references resolve through domain-indexed fact demands (13.2). License = three conditions, each read by
existing machinery: **(1) element provenance** — every element of every successor state comes from
already-interned **pooled leaf values** (the literal arguments' leaves ∪ the leaves operations actually insert from body constants [R4 wording]), read off the transfer rules'
own part/recombination descriptions; `f = x => f([x])` fails natively (the new container's element is the
prior *state*, not a pooled atom). **(2) total-length drift ≤ 0 per recursive arm** — the ordinary tier-2
drift reading applied to the state's summed length (`rest` −1, `++ ["A"]` +1, per-arm constant from the
arm's text); `g = l => g(l ++ [0])` fails (+1); drop-a-letter-add-"A" passes (0). **(3) scope — the
complete varying call state [R5]:** every recursively *varying* argument is a flat sequence under
(1)–(2); every other argument is **proven constant** across the recursive edges (transfer-readable);
recursive control location (function/row — mutual recursion) ranges over the finite program-derived graph
and enters the bound as a finite factor **Q**. `f = (xs, n) => f(xs, n + 1)` is out — `n` varies and is
not a sequence (and `n + 1` exits the pool): the license is inapplicable, this candidate contributes
nothing — under a loose reading the walk would neither end nor repeat, breaking 13.3 itself, which is why
completeness of the covered state is a license condition, not a nicety. The finite-product extension
(non-sequence varying components with proven finite exact domains multiplying the bound) is **recorded,
not v1**. Nesting stays out by provenance, not by a size metric. **(4) semantic-state stability [R6]:**
every semantic dependency capable of affecting arm selection, recursive transfer, or base reachability —
pending Mutator write-sets (read-your-writes, B5), box contents, world results, seat facts — must be
**fixed across the chain** or part of the certified finite state; **v1 scope: pure/snapshot-stable
recursion only**; Mutator/Effect chains contribute no conclusion unless every such dependency is proven
invariant. Counterexample [R6]: `@mutable n = 0; @mutate f = xs => { n := n + 1; n == 10 ? done : f(xs)
}` — the key (control, xs) repeats every iteration while pending `n` advances 1, 2, 3, …; ignoring it
would mint a **false closed-orbit refutation of a terminating program** — the worst verdict class. The
late-resolution dependency law, applied to the chain's own state key; no mutable-state carrier is
invented.
Under (1)–(4) the reachable **semantic** states are represented by finitely many flat argument arrangements and finite control locations, with every remaining semantic dependency fixed [erratum, final round] — **finiteness provable in advance** (13.3(1); no cardinality is ever computed), steps
deterministic (**exact unique row selection is an explicit per-step condition [R4]** — an exact input does not decide an opaque predicate; a non-exact selection ends this candidate's contribution; one arm per step when exact) — so by pigeonhole the walk has exactly two endings:
base reached (grounding proven; §12.2 folding then licensed, in that order) or a state revisited (closed
orbit; refuted with the orbit as jointly-represented witness). No third ending; no budget. `collatz(27)`
fails (1) (`3n+1` outputs are not parts or pooled constants) → this mechanism contributes nothing —
candidate-local, never a verdict by itself. Certification is bounded by body + literal size; **the walk is bounded by a computable combinatorial function of (pool size P, fixed arity A, initial total length L)** — e.g. states ≤ Q · Σ_{k≤L} C(k+A−1, A−1)·P^k — advance-computable per 13.3(1), **owed to §16 as the bound theorem**; the implementation never computes it and never uses it as fuel [R4].

**2.4 Multi-base — fixed exit only** [R1 high 7, aligning with the user's alternating-specimen ruling]:
progress must be monotone toward **one fixed exit** (or a fixed exit per domain partition, or one combined
certified measure). Existential per-step target switching is forbidden: 40→60→40 between exits at 0 and 100
improves toward *some* exit every step and loops forever.

**2.5 Numeric grounding certificate v3 — two independent components** [R1 blocker 6; R2 high 4].
**(1) Well-founded descent:** recursive transitions strictly progress in a well-founded order. Dense
(rational) measures additionally require a **call-derived positive progress floor δ** with a maintained
ranking domain — each run finite by the Archimedean argument; the shrinking-step specimen
(`f(x+s, s/2)`) has no derivable floor and is honestly unproven. **(2) Landing/coverage:** the transitions
cannot skip the applicable base — region/half-line bases discharge landing structurally; **point bases
retain the Mod/grid alignment obligation**; finite-interval coverage and domain partitions as in the C§10
core. Non-vanishing progress prevents Zeno; it never proves landing: `f = n => n == 0 ? done : f(n − 2)`
from `Equals(1)` has constant drift, positive magnitude, a point base — and skips it (1, −1, −3, …),
**refuted by the grid** (1 ∉ the even lattice). The C§10 machinery always carried this; v2's
"intrinsically well-founded" sentence wrongly dropped the landing half and is narrowed to: integer
measures with strict constant drift are intrinsic **for region bases or grid-aligned point bases only**.
**C§13.3 bounds the compiler's symbolic proof procedure — never runtime recursion depth** (`countDown` on
`GE(0) ∧ Mod(1,0)` proves with unbounded depth). Exact-state following separately requires §2.3's
native exact-chain finiteness license.

## 3. RULE — world-decided classification [promoted from candidate: user D-β, 2026-07-29; completion
semantics derived from existing law + ruled D-α]

**The classification [rule]:** grounding unproven AND every recursive-arm selection gated by a world value
of the recursion's own activations — produced in the current activation or carried in through the recursive
arguments (`loop(msg) => msg == "quit" ? done : loop(read())`) → **world-decided**: excused from exactly
one thing, a bound on the number of world-driven iterations. Each activation's own computation completes
(below). **v1 source law [D-α]:** since every effect primitive is total-return, world-decidedness has
exactly one origin — world-gated recursion, directly or transitively; **every finite effect segment of
total-return primitives and proven-completing calls completes** [scoped R4 — a straight-line call to a
world-decided callee may itself not return].

**Completion semantics, the five derived pieces:** (1) **Seats** — by B5's admission matrix (pure calls
pure only; mutation never calls Effects), world-decided callees can exist **only at effect-world seats**;
the seat question dissolves by jurisdiction. (2) **Theorems (leaning-conditional):** the pure world is
total; every compiled Mutator completes — **the single mutation branch can never hang** (the 1.0.9 sharp
corner, dissolved as predicted). (3) **Downstream = sequencing:** code after a world-decided call is
world-conditioned (shutdown/continuation code); **no contract-side change** — produced contracts always
described produced values; reachability was never a value-machinery quantity. (4) **Propagation:** one
transitive `WorldDecided` label riding the existing instance-metadata channel (the act-kind channel,
C§13.2), judged by the application package's **existing three-valued completion evidence at each seat** —
the seat judgment table gains one row (effect-world seats admit world-decided evidence with the
downstream-conditioning consequence); **the row consumes a classification — it never establishes one**
[R4]; that one-row amendment to the design-closed application spec rides the grounding spec's landing,
flagged, never silent. (5) **Failure orthogonal, the import barred by B6:**
Failure = completed-and-the-world-said-no (a value; "no hidden control transfer, ever"); world-decided =
may-never-complete (no value, nothing downstream). A reified `DidNotComplete` is unconstructible under
B6's inert-failure law; timeouts are author-written world design. Segment grounding needs no procedure —
clause 1 binds every pure sub-computation; primitives are total [D-α].

**D-α [user, 2026-07-29]: all effect primitives are total-return** — success or `Failure`, never blocking;
long-running interaction belongs to the deferred async layer. v1 consequence: world-driven loops **poll**
(Failure = "nothing yet", re-polled by the recursion) — efficiency for the async layer, semantics
unchanged. **Net theorem [reworded R4]: under P-1's leaning, machine limits aside, the sole admitted source of
semantic *non-completion* in compiled NEXT is a soundly classified world-decided recursion in effect
world.** Proven-terminating recursion may still run to unbounded *depth* over an unbounded input contract —
non-completion and unbounded depth are different quantities (the Zeno round's conflation, caught again).

**[R4] The sound classifier — OWED to Grounding Spec v1; the paragraphs above state the ruled POLICY, not
the recognizer.** Two counterexamples defeat the prior wording: **stale carried value** —
`loop(msg) => msg == "quit" ? done : loop(msg)` with world-born `msg`: one observation at entry, then a
purely internal infinite loop, wrongly exempted; **decorative branch** — `bit = readBit();
bit ? loop() : loop()`: fresh observation each cycle, but both arms recurse — the world picks a branch,
never completion. Sound conditions: **(a) every recursive cycle crosses a fresh world observation** — a
carried parameter counts only when syntactically refreshed from an Effect result on every recursive edge
(`loop(read())` survives; `loop(msg)` dies); **(b) the world-may-reach-completion closure covers the full represented SCC domain [R6 — replacing the
existential exit, which round 6 defeated]:** seed the jointly realizable completing transitions;
propagate **backward** through jointly realizable world-conditioned transitions and internally grounded
finite segments (the −4-trap backward move's structure, on the classifier's own finite domain/control
graph); the closure must cover **every represented recursive state** of the admitted SCC domain.
Quantifier shape: *for every represented state s, some realizable finite sequence of fresh world outcomes
reaches completion* — then any infinite run reflects the world actively avoiding completion forever,
which is what "world-decided" must mean. One exitable inhabitant admits nothing: `loop(mode)` at
`mode ⊑ Number` — the pair (mode = 0, bit = true) is realizable, yet every `mode ≠ 0` state reads fresh
bits forever with no possible completion — internally doomed inside a mixed domain; the closure covers
only `{mode == 0}`, so the broad call is **not** world-decided (unproven, blocking under the leaning; the
caller narrows to `mode == 0`, which classifies cleanly). The admission is thereby **universal over the
domain**, structurally parallel to grounding's universal obligation — the same quantifier discipline
round 2 established for refutation, now on the admission side. **Clean v1 classifier shape
[R5-endorsed]:** per domain-indexed recursive SCC — mark every edge crossing a qualifying fresh world
observation; the remaining internal graph must be acyclic; the world-may-reach-completion closure covers the full admitted SCC domain [R6]; a carried parameter qualifies only when refreshed from an Effect result on every
recursive edge; **no general provenance/taint propagation in v1**. Admission stays bounded and syntactic;
the grains used (instance, row/domain, region table, fact-graph SCC) all exist.

## 4. INTEGRATION CONDITIONS / OPEN SPEC OBLIGATIONS

1. **Tuple absence-form verification** (Claude Code): `Repeat(NotEquals(v))` preserved under
   `rest`/descent by the closed procedures. Gates §2.2.
2. **Basin derivation — DEFERRED [user, 2026-07-29]; nothing to write for v1.** Later-improvement sketch,
   recorded so re-entry is by the front door with the price visible: a fixed step-kind ↦ orbit-shape table
   (×r ↦ `Geo`; ±d ↦ grid; prepend-C ↦ `Repeat`-form; composed lap ↦ orbit of the net step), parameters
   read off base and arm — no synthesis, no composition depth (a depth bound is a budget wearing a suit —
   Principle 7); certification by the existing closure checks (trigger arm refuted on the region; step maps
   inside); per-call membership. Cost: new rule inventory, one soundness proof per row; the Geo detail
   tables are a real dependency. Killed framing stays killed: template-grammar search over composed
   candidates (invariant-synthesis import).
3. **Exact-chain license — CONTENT DRAFTED in §2.3, native form** [2026-07-29; rewritten after the
   author's import audit]; remaining: placement in Grounding Spec v1 + round scrutiny.
5. **Effect completion — POLICY RULED [user D-α/D-β]; SOUND CLASSIFIER OWED [R4]** (§3). Owed to Grounding
   Spec v1: fresh-world-observation-per-recursive-cycle recognition (designated parameter syntactically
   refreshed from an Effect result on every recursive edge, or a guard on a current-activation result);
   stale-carried-value rejection; the world-may-reach-completion closure covering the full admitted SCC
   domain [R6]; the bounded
   syntactic/dataflow admission rule (general provenance/taint metadata deferred). Mechanical: the
   total-return inventory column; the one seat-table row (consumes, never establishes).
6. **General affine numeric basins** beyond Geo/grid orbits — open; not v1.
7. **Witness-candidate inventory — DISSOLVED [user, 2026-07-29]:** ruled **empty** — the analyzer never
   constructs witnesses; a witness is only ever a call's own written argument (§2.3 covers those). Wide
   calls: procedure-relative unproven, basin-powered diagnostic. One sentence for the spec; nothing else
   owed.
7b. **Program-expressed measures — CONTENT DRAFTED [2026-07-29, from the author walkthrough; remaining
   owed: lifting into Grounding Spec v1 + its round scrutiny].**
   **[Import-audited 2026-07-29 — survives.]** Lineage per component: admission = the author's
   canonicalization ruling and base–measure connection (base arms only; finitely many; **no synthesis or
   coefficient search — the guard-mining-plus-solving frame of ranking-function tools is exactly what this
   is not**); drift = the polynomial normalizer; coincidence = a restriction [user-caught]; landing = the
   grid discipline; range check = ordinary forward transfer + subcontract. **Honest limit:** per-call
   ranges computed from separate argument contracts lose correlation (`a + b == 5` cannot be conveyed —
   relational, [permanent]); the check fails conservatively there, never falsely passes.
   **Admission:** the measure candidates at a stop are the canonicalized expressions its base arms test —
   bare arguments and compound expressions alike (`n`; `2a+b`). Finitely many per instance (base arms are
   finite); no other expression in the body is ever a candidate; the analyzer never invents quantities
   [user].
   **Drift derivation:** per recursive call *as written*: substitute the call's argument expressions into
   the candidate and normalize — `2(a−1)+(b+1) = 2a+b−1`. One algebra computation per textual recursive
   call, cached per instance; zero runtime cost; no unrolling, no evaluation (§12.2 untouched); analysis
   cost proportional to source size, never to run length.
   **Floor–stop coincidence rule [user-caught]:** the route proves only when the stop *is* the candidate
   reaching its floor (`2a+b == 0` alone). A stop with additional conjuncts (`&& a == 0`) → unproven:
   descent to the floor does not establish stopping, and whether the extra conjuncts hold at reachable
   floor states is relational — outside [permanent]. Witness: the (1,−2) state (F-1) — floor reached, stop
   unfired, descent continues forever; the stricter-stop variant genuinely never terminates, so the
   conservatism is truth.
   **Landing and the derived input condition on the expression:** the two-component certificate applies to
   the candidate itself — the tier-2 core's own derived input condition, factorial-style (distinct from
   the deferred orbit-basin machinery): `2a+b ⊑ GE(0) ∧ Mod(1,0)`; checked per call by computing the expression's **range** from
   the arguments' contracts forward through the operation rules: a unary computed range, never a stored
   relation.
   **Coverage (author-facing; the analyzer never transforms programs [user question, settled]):** two
   author routes. (1) *Recompute at the stop* — machine-guarded (every future edit re-checked by the drift
   recomputation), narrow (floor-is-the-stop functions only). (2) *Carry as an argument* — universal;
   termination proven via the counter; the counter's correspondence to the intended quantity is
   inexpressible ([permanent] relational perimeter) and author-maintained — a ranking coordinate, not a
   verified invariant. What fits neither route: unproven — warning today, error under the leaning.
   Rewriting a function into its counter-carrying twin is the analyzer inventing quantities and
   constructing a different program — forbidden.
8. The one-sentence fact-chain line for the grounding spec (13.3(1) restatement, now in §2.3's terms).

## 5. KILLED (carried + new; do not resurrect)

Tier-0 evaluation-as-grounding; the structural-bound knob; base-emits-measure as universal;
region-base-no-landing unconditional; for-every-k parametric framing; deltas-declared-on-operations; the
supplied-measure surface feature; wide preserved-trigger presence refutation; **"conserving operations" as
a carrier license** [R1]; **gap/δ as the licensing theory** [R1]; **"toward any one exit" without
fixed-exit discipline** [R1]; **the ProgressStamp slot claim for function grounding** [R1]; **"prove
absence never presence" as universal epistemics** [R1]; **constructed witness inventories —
candidate-input generation, a test-generation import** [user, 2026-07-29]; **the generic
state-carrier/node-count/cardinality-N framing — explicit-state model-checking import; the native license
is element provenance + total-length drift in tuple-family vocabulary** [user audit, 2026-07-29].

## 6. SPECIMENS (the regression seed)

Zeno `f(x+s, s/2)` · the 7-literal four-way test (literal-with-7 refuted / literal-without-7 folds-after-grounding / **wide `Repeat(Number)`: unproven — no constructed witnesses [user]; the diagnostic names the blocking arm (the `== 7` merge arm; remedy-region lines return with the deferred derivation); the literal call `f([3,7,2],[])` refutes, its written argument itself the witness; mixed fates alone decide nothing [R2]** / filtered-proven) · weighted-sum landing failure (F-1) · Ackermann joint lex · filtered absence basin ·
`collatz(64)`/`collatz(27)` — both unproven in v1 (derivation deferred [user]); `Geo(1,2)` recorded as the later-improvement's numeric row · McCarthy 91 band chains · the oscillator
via lap composition · `countDown` wide-domain (well-founded without depth bound) · `f = x => f([x])`
(structural minting) · 40↔60 (fixed-exit) · `f(n−2)` from 1 (landing ≠ descent; grid-refuted) · `loop(msg)` (carried world value) · the expecting-seat
noncompletion case.

---

*v4 — six hostile rounds integrated (R1: six blocker-level corrections + the Geo pushback sustained;
R2: witness semantics, basin locality, two-component certificate, witness-path forcing; R3: confirmatory
+ the D-6 ruled-vs-owed split; R4: the WorldDecided classifier restored as owed, the exact-chain bound
theorem, the non-completion rewording; R5: domain-indexed reachable exits, complete-varying-state
finiteness with the Q factor, terminology syncs; R6: the completion closure covering the full SCC domain,
semantic-state stability for exact chains). All [user] rulings preserved at their original
strength; assistant overclaims demoted where found. The grounding specification drafts from this note;
§4 lists what it must discharge — compendium C§17 [1.0.16] registers the same package.*
