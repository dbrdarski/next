# Grounding Specification v1 — Fresh-Session Handover (2026-07-29)

**Purpose.** Start Grounding Spec v1 drafting in a clean session. Inputs, all in the canonical library
(verify against `MANIFEST.sha256.txt` before trusting any copy — the stale-upload guard):
`next-termination-decisions-v4.md` (six hostile rounds + final confirmatory ACCEPTED + author approval —
the ratified pre-spec source), `next-design-compendium-v1-0.md` (patch 1.0.17 — the synchronized master;
C§17 registers the same package), `next-late-resolution-v0-5.md` (design-closed; C1–C8 is required method).

**The v1 package to specify (C§17 / decisions §4 — the eight items):**
1. Numeric grounding: well-founded descent + landing/coverage (two independent components; §6 Zeno; the
   floor–stop coincidence rule [user-caught]).
2. Exact singleton fact chains under the native finiteness license: exact unique row per step; pooled flat
   leaves; non-growing total length; complete varying call state (others proven constant; control factor
   Q); semantic-state stability (v1 = pure/snapshot-stable); the §16 combinatorial bound theorem.
3. Lexicographic candidate inventory + the joint-settlement theorem's §16 discharge.
4. The program-expressed measure read path (7b: admission = base-tested canonicalized expressions; drift
   by substitute-and-normalize; coincidence; range check; the coverage/perimeter paragraph — content
   drafted, lift and integrate).
5. Witnessed drift-away / exact-orbit refutation over already-represented exact call values (never
   synthesis [user]).
6. The WorldDecided classifier: per-instance/domain recursive SCCs; fresh world observation on every
   cycle; stale-carried rejection; the world-may-reach-completion closure covering the FULL represented
   SCC domain; bounded syntactic admission (no taint metadata).
7. Mechanical Effect integration: total-return primitive inventory column [D-α]; the one seat-table row
   (consumes a classification, never establishes one) as a flagged amendment to the application spec.
8. Tuple absence-form preservation for the filtered structural case (also a standing Claude Code
   verification task).

**Killed — do not resurrect (decisions §5):** tier-0 evaluation; budget/effort knobs; for-every-k
parametric facts; deltas-on-operations; supplied-measure annotations; presence refutation; constructed
witness inventories; generic state-carrier/node-count framing; invariant-synthesis basin search; general
world-provenance/taint metadata; "toward any exit" without a fixed exit.

**Deferred by ruling [user]:** automatic basin derivation (sketch + cost sheet in decisions §4);
finite-product exact-chain extension; async/non-polling Effects (with the reactive fence).

**Open, non-blocking:** P-1's two policy picks (hard-vs-acknowledgeable; the [permanent] families) —
draft under current law, per-class verdict policy carries the 1.0.9 status; the Claude Code task (item 8).

**Process discipline (binding):** C1–C8 per late-resolution v0.5; import-audit every drafted rule
(lineage per component); examples traced before use; example changes announced as corrections;
"verified" reserved for §16 discharge; every ledger change flagged **LEDGER CHANGE**; provenance tags
([user] only for actual rulings); policy/procedure split before any "closed" claim; post-patch
killed-term + stale-marker (range-aware) verification; hostile rounds until confirmatory; the author
stamps closures.
